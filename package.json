{
  "name": "agenthub",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "dev": "pnpm --recursive --parallel dev",
    "build": "pnpm --recursive build",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "pnpm --recursive typecheck",
    "typecheck": "pnpm --recursive typecheck",
    "db:seed": "pnpm --filter @agenthub/core-engine db:seed",
    "e2e": "echo 'Playwright E2E — coming in M3'",
    "load-test:health": "k6 run tests/load/health-check.js",
    "load-test:agents": "k6 run tests/load/agent-listing.js",
    "load-test:message": "k6 run tests/load/message-send-sse.js",
    "load-test": "pnpm load-test:health && pnpm load-test:agents && pnpm load-test:message"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}

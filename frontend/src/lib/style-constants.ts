/**
 * @fileoverview Shared style constants used across dialog and form components.
 *
 * Extracted to a single source of truth to eliminate repetition across
 * conversation-create-dialog, orchestrator-dialog, and other form components.
 */

export const FOCUS_CLASS =
  'focus-visible:ring-0 focus-visible:border-primary/50 focus-visible:shadow-[0_0_0_1px_hsl(var(--primary)/0.15)] transition-[border-color,box-shadow]';
